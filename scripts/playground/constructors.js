const my_str = String(); //Returns an empty string
let my_array = Array(); //Returns []
const my_obj = Object(); //Returns {}
const my_int = Number(); //Returns 0
const my_bool = Boolean(); //Returns false; use "!" to return true
const my_date = Date(); //Returns today's date
const my_regexp = RegExp();
const my_func = Function(); //Returns an anon function

my_array.push("a");

my_array = Array(); //Empties out the array